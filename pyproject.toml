[project]
name = "osint-harvester-passive"
version = "0.2.0"
description = "Passive OSINT harvester with risk scoring and reporting"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
